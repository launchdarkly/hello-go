module github.com/launchdarkly/hello-go

go 1.13

// This file has been deliberately checked in without any "require" details, so that
// the Go compiler will pull in the latest versions of the SDK and its dependencies
// when the application is built.
